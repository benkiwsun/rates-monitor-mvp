# Copy to Streamlit Community Cloud: App settings → Secrets (TOML format)
# Or locally: save as `.streamlit/secrets.toml` (do not commit)

DATABASE_URL = "postgresql://user:pass@host:5432/rates_monitor"
FRED_API_KEY = "your_fred_api_key"
USE_SAMPLE_FALLBACK = "true"
SCHEDULER_ENABLED = "false"
# ADMIN_INGEST_KEY = ""
